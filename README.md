# Agent Skills

Agent Skills provides reusable .NET services for building, materializing, installing, exporting, and diagnosing agent skill packages.

Product repositories own their skill definitions and generated packages. This repository owns the common package format and host-specific materialization rules.
