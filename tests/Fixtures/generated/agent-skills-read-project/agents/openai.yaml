interface:
  display_name: "Agent Skills Read Project"
  short_description: "Use when inspecting a Unity project with Agent Skills before choosing selectors, operation metadata, schemas, or readIndex freshness. Guides read-first discovery without hard-coding operation contracts."
  default_prompt: "Use $agent-skills-read-project to follow the Agent Skills Read Project workflow."

policy:
  allow_implicit_invocation: true
