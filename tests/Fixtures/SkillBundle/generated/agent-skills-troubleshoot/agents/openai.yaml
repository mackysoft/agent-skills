interface:
  display_name: "Agent Skills Troubleshoot"
  short_description: "Use when diagnosing Agent Skills daemon, lifecycle, readIndex freshness, timeout, compile or domain reload, logs, IPC, and Unity execution failures."
  default_prompt: "Use $agent-skills-troubleshoot to follow the Agent Skills Troubleshoot workflow."

policy:
  allow_implicit_invocation: true
