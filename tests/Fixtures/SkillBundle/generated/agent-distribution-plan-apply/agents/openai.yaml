interface:
  display_name: "Agent Distribution Plan Apply"
  short_description: "Use when building, validating, planning, and applying a Agent Distribution JSON request for Unity changes. Enforces read -> describe -> build request -> validate -> plan -> call -> verify."
  default_prompt: "Use $agent-distribution-plan-apply to follow the Agent Distribution Plan Apply workflow."

policy:
  allow_implicit_invocation: true
