interface:
  display_name: "Agent Distribution Read Project"
  short_description: "Use when inspecting a Unity project with Agent Distribution before choosing selectors, operation metadata, schemas, or readIndex freshness. Guides read-first discovery without hard-coding operation contracts."
  default_prompt: "Use $agent-distribution-read-project to follow the Agent Distribution Read Project workflow."

policy:
  allow_implicit_invocation: true
