interface:
  display_name: "Agent Distribution Troubleshoot"
  short_description: "Use when diagnosing Agent Distribution daemon, lifecycle, readIndex freshness, timeout, compile or domain reload, logs, IPC, and Unity execution failures."
  default_prompt: "Use $agent-distribution-troubleshoot to follow the Agent Distribution Troubleshoot workflow."

policy:
  allow_implicit_invocation: true
